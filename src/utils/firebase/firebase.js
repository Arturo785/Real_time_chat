import firebase from "firebase/app"


const firebaseConfig = {
    //Use your own fireBaseConfig realDataBase in here
  };

  //Para poder ser usada
export default firebase.initializeApp(firebaseConfig);